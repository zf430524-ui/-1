# PRD 正式审查记录

- 审查对象：`docs/prd.md`
- 审查日期：2026-09-05
- 审查结论：候选版已补全，状态为 `REVIEW`；等待用户确认 6 项默认决策后方可冻结。
- 审查方法：依据 `$feeling` 的 PRD 审查流程，先处理边界、状态、数据与验收问题，不进入代码实现。

## 必须修改（本次已完成）

1. **澄清 v1 与 v1.1 的住宿边界**：v1 保留手工房源、建议、调整、校验和预览；批量导入与完整分房表导出归入 v1.1。
2. **区分原型与生产系统**：原型为本地模拟流程，真实证件、登录权限、云端协作和审计属于生产化阶段。
3. **补齐角色与数据可见范围**：明确负责人、调度工作人员和兼职导师的职责与权限。
4. **补齐状态与异常语义**：明确项目、报名、退出、替换、锁档及批量部分成功的处理方式。
5. **补齐隐私和安全边界**：原型禁止录入真实证件；生产版需要加密、授权、审计、备份和删除机制。
6. **补齐可验证验收标准**：为日期冲突、车辆、住宿、输出、响应式和数据一致性定义可测结果。
7. **纠正任务顺序**：PRD 未确认前不生成最终开发清单，也不把 UI 首页实现排在应用骨架之前。

## 建议后置

- 微信群读取、机器人接龙和自动通知。
- 保险公司接口与真实投保。
- 多旅行社租户、学校端、车队端和司机端账号。
- 工资、支付、发票与结算。
- 导游信用自动扣分、抢团排序或学校口碑算法。
- 路线优化、完全自动派团和无人值守发布。
- 复杂教师房、楼层偏好及模板级 Excel 复刻。

后置原因：这些能力要么依赖外部平台和合规条件，要么会显著扩大权限、隐私与规则复杂度，不能在核心派团流程尚未验证前混入首版。

## 可接受但应记录的决策

- 自动车辆和住宿编排定位为“建议＋人工调整＋持续校验”。
- 原型可用浏览器本地存储验证流程，但不能承载真实业务敏感资料。
- 视觉方向采用已选定的“运营指挥台”，Figma 写入阻塞不影响先冻结产品规则。
- v1 表格输出以字段、合计和业务结构匹配为验收，不以图片像素复刻为验收。
- 首版不根据主观“团好不好带”给导游自动排序，避免不可解释的派单结果。

## 待用户确认

1. 导游默认可见完整的基础招募信息。
2. 录取后还需导游确认，确认时才锁档。
3. 已确认退出要审批和留痕，但首版不自动扣分。
4. 车辆安全余座默认 0，可按项目调整。
5. v1 表格只做结构一致，精确 Excel 模板后置。
6. 住宿基础流程纳入 v1，批量能力和正式导出归 v1.1。

## 审查门禁

- 用户全部同意：PRD 改为 `APPROVED`，更新决策记录，然后生成正式 `tasks.md`。
- 用户要求修改：PRD 保持 `REVIEW`，只修改有分歧的条目并再次确认。
- 在 PRD `APPROVED` 前：不进入应用骨架、UI 实现或业务代码。
