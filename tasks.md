# 当前流程任务

> 本文件目前只记录产品定义阶段。按照 `$feeling` 的顺序，PRD 通过后才会重建为正式开发任务清单；现有开发任务草案已撤回，避免在需求未冻结时误开工。

## 状态说明

- `TODO`：尚未开始。
- `IN_PROGRESS`：正在实施，尚未完成验收。
- `BLOCKED`：被明确依赖阻塞。
- `REVIEW`：文档或机器验收已完成，等待人工确认。
- `DONE`：约定验收已通过。

### T01 · 建立工程契约

- 状态：DONE
- 目标：固化首版业务基线、关键假设和交接入口。
- 输出：README、轻量 PRD、决策记录和初始任务清单。
- 验收：文档存在并能回答首版业务方向。
- 不做：页面和业务代码。

### T02 · UI/UX 发现与范围锁定

- 状态：DONE
- 目标：明确用户、页面范围、视觉方向和设计依赖。
- 输出：UI/UX 设计简报、Figma 文件和差距记录。
- 验收：首批页面范围、视觉原则和阻塞条件清楚。
- 不做：正式页面和前端代码。

### T03 · UI 视觉方向预览与选择

- 状态：DONE
- 目标：通过三套预览选择工作台的视觉语言和信息层级。
- 输出：三张预览，用户选择第 1 张“运营指挥台”。
- 验收：视觉方向已由用户明确选择。
- 不做：交互原型和前端代码。

### T04 · 正式 PRD 审查与补全

- 状态：REVIEW
- 目标：把轻量草稿升级为可供开发评审的 PRD 候选版，识别必须修改、建议后置和待确认决策。
- 输入 / 依赖：T01—T03、用户原始业务说明、乘车安排与住宿表参考图。
- 输出：`docs/prd.md`、`docs/prd-review.md`、`docs/decisions.md`、修正后的 `README.md` 和本任务状态。
- 文件范围：产品文档与流程任务记录，不含应用代码。
- 验收：版本边界、角色、流程、状态、数据、安全、验收和未决事项均有明确记录；不存在虚假的运行命令；用户可一次确认推荐默认值。
- 预计：60 分钟
- 风险：锁档、退出、安全余座和表格复刻标准需要用户确认。
- 外部依赖：无
- 不做：不生成最终开发清单，不制作 UI，不编写代码。

### T05 · 确认并冻结 PRD

- 状态：TODO
- 目标：处理用户对 6 项推荐默认值的反馈，把 PRD 状态改为 `APPROVED`。
- 输入 / 依赖：T04、用户确认或修改意见。
- 输出：冻结的 PRD 与已确认的决策记录。
- 验收：`docs/prd.md` 不再含待确认产品决策，相关决策均标记为已确认。
- 预计：30 分钟
- 风险：若业务规则有分歧，需要小范围修订后再次确认。
- 外部依赖：用户确认。
- 不做：不编写代码。

### T06 · 生成正式开发任务清单

- 状态：BLOCKED
- 目标：依据已冻结 PRD，把工作拆成约 30—60 分钟且可独立验证的任务。
- 输入 / 依赖：T05 完成。
- 输出：重建后的 `tasks.md`，每项含状态、目标、输入、输出、文件范围、验收、风险、外部依赖和不做事项。
- 验收：依赖顺序正确；应用骨架、领域规则、各业务页面、导入导出和 QA 均可逐项交付；没有 180 分钟的大任务。
- 预计：45 分钟
- 风险：任务粒度需与最终技术路线一致。
- 外部依赖：PRD 必须先获用户确认。
- 解除条件：T05 状态变为 `DONE`。
- 不做：不在拆任务时顺手实现功能。
