# 研学团负责人工作台 PRD

- 版本：v1.0 重写候选版
- 状态：`REVIEW`
- 更新日期：2026-09-08
- 目标形态：公司本地电脑上的单机负责人工作台
- 产品负责人：旅行社研学团负责人
- 需求基线：`../研学团负责人工作台整合思路.md`
- 功能规格：`页面字段与交互规则清单.md`

> 本 PRD 取代 2026-09-05 版本中“负责人端＋导游移动端”的双端方案。首版只建设旅行社负责人使用的本地工作台；导游招募、微信群二维码和接龙继续在微信中人工完成。本文件获确认前为候选需求，不据此宣称现有 Demo 已完成交付。

## 1. 产品概述

研学团负责人工作台是一套围绕“单次研学项目”组织资料、人员和执行安排的本地业务工具。

负责人可以先用学校、年级、目的地和日期建立项目，再逐步导入或补充班级、老师、导游接龙、车辆、住宿和用餐资料。系统负责复用学校与导游档案、检查日期和容量冲突、给出初步安排建议、允许负责人手工调整，并从当前项目数据生成保险、乘车、住宿和用餐 Excel 表格。

产品不替负责人做最终业务决策。它的核心价值是：让同一项目中的人数、资源、异常和输出使用同一套数据，减少微信群接龙后的重复统计、人工核对和多张 Excel 反复修改。

## 2. 背景与问题

当前研学团组织主要依赖微信群、学校或供应商提供的 Word/Excel，以及负责人手工维护的多张执行表。典型问题包括：

1. 同一批学校、班级、老师和导游资料需要在不同表格间重复复制。
2. 接龙名单与导游档案无法稳定对应，同名、缺手机号或资料不一致容易绑定错误。
3. 导游可能在日期重叠的项目中被重复安排。
4. 班级人数变化后，车辆、住宿、用餐和输出表不会自动同步。
5. 车辆超载、住宿容量不足、男女混住、餐桌不足等问题依赖人工发现。
6. 导游证件和保险字段分散，导出前需要逐人查找。
7. 导入、调整和导出缺少可追查记录，出现错误时难以确认来源。
8. 本地业务数据和证件图片缺少明确的备份、恢复和敏感信息保护路径。

## 3. 产品目标与成功定义

### 3.1 产品目标

1. 建立学校库、导游库和项目业务数据三个逻辑模块，使长期资料可复用、项目资料不串项。
2. 以班级人数为统一基线，联动车辆、住宿、用餐、异常和 Excel 输出。
3. 把导游接龙文本转换为可核对的项目名单，并阻止日期重叠、证件无效或停用导游被安排。
4. 把车辆、住宿和用餐自动化定位为“系统建议＋人工调整＋持续校验”。
5. 让负责人能从当前项目数据生成最新执行表格，而不是维护与项目脱节的独立表格副本。
6. 对敏感资料查看、数据导入、安排修改、表格导出、备份和恢复提供可追查记录。

### 3.2 v1 功能成功标准

- 能用最少字段建立项目，并在资料逐步补充时明确显示缺失项和下一步。
- 班级男女生人数、班级总人数和项目总人数始终可校验、可追溯。
- 接龙名单保持原始顺序；姓名与手机号一致时可匹配导游档案，歧义项进入人工确认。
- 日期闭区间重叠、证件过期和合作停用能阻断导游分配。
- 车辆安排能够识别超载、未安排、拆班人数不平和导出字段缺失。
- 多日住宿能够识别容量不足、房间超员、男女混住、人员遗漏和班级人数不一致。
- 含餐项目能够显示学生、导游、老师、司机人数，计算桌数建议并识别桌数或人员安排缺口。
- 导游、车辆、房间、餐桌或班级人数变化后，关联统计、异常和表格预览同步失效并重新计算。
- Excel 实际生成、能够打开且中文不乱码；页面提示不能代替文件验证。
- SQLite 数据和证件目录能够完成可验证备份；恢复前保护当前数据。

### 3.3 试用期观测指标

首版不承诺未经基线验证的效率提升比例。建议先用 3—5 个脱敏或真实授权项目建立基线，再设目标。

| 指标 | 定义 | 观察时点 |
| --- | --- | --- |
| 项目准备耗时 | 从资料首次录入到完成安排的人工用时 | 每个项目完成安排时 |
| 重复录入次数 | 同一字段被跨表重新手工录入的次数 | 项目复盘时 |
| 导出后返工次数 | 已导出后因数据不一致重新修改的次数 | 项目结束时 |
| 提前发现异常数 | 系统在导出前发现的日期、人数、容量或字段问题 | 每次导出前检查时 |
| 导入修正率 | 需要人工修正的导入行数 / 总导入行数 | 每个导入批次完成时 |
| 恢复成功率 | 抽检备份可成功恢复的次数 / 抽检次数 | 定期恢复演练时 |

## 4. 用户、场景与权限边界

### 4.1 用户

| 用户 | 主要任务 | v1 权限 |
| --- | --- | --- |
| 旅行社研学团负责人 | 建项目、导资料、安排导游/车辆/住宿/用餐、处理异常、导出表格、备份数据 | 操作本机全部业务数据；按规则查看和导出敏感资料 |

首版不建设调度员工、导游、学校、车队、司机、酒店或餐厅账号，也不提供多角色在线协作。

### 4.2 核心使用场景

1. 学校先给出少量行程信息，负责人快速建立项目。
2. 学校计划确定后，负责人导入或补充班级、人数和老师资料。
3. 负责人生成招募通知并复制到微信群；接龙结束后，把文本复制回工作台。
4. 系统匹配导游档案并检查日期、证件和合作状态，负责人完成班级分配。
5. 负责人导入或填写车辆、司机、房间和餐桌资料，使用建议后手工调整。
6. 系统集中显示阻断和警告，负责人逐项修正并重新校验。
7. 负责人预览并导出保险名单和乘车表；按项目条件导出住宿表和用餐表。
8. 负责人定期备份本地数据，在必要时从已验证备份恢复。

### 4.3 权限和设备前提

- 工作台部署在公司受控的 Windows 电脑上。
- “单机单人”不等于可以无保护存放真实身份证、银行卡或证件图片。
- 在真实敏感资料进入系统前，必须确定并验证本地访问控制方案：由专用 Windows 账户隔离，或增加应用解锁机制。
- 未完成访问控制、备份权限和恢复验证前，只允许使用模拟或脱敏数据。

## 5. 产品范围

### 5.1 v1 包含

1. 今日工作台、项目列表、新建/编辑项目和项目执行总览。
2. 班级、学生聚合人数、班主任和配班老师管理。
3. 学校库及常用联系人管理。
4. 导游档案、证件有效期、合作状态、带团及退团事实记录。
5. 微信群招募通知文本生成、复制和最终接龙文本导入。
6. 接龙顺序保留、导游档案匹配、歧义确认、日期冲突和班级分配。
7. 当前项目内的车辆、司机和乘员安排；容量校验和人工调整。
8. 条件启用的住宿和用餐安排；建议、人工调整和异常校验。
9. Excel、Word、粘贴文字和手工录入入口；字段映射、预览确认和导入记录。
10. 保险名单、乘车安排表以及条件启用的住宿表、用餐表预览与 Excel 导出。
11. 异常中心、敏感操作记录、导入导出记录。
12. SQLite 数据库和证件图片目录的本地备份与恢复。

### 5.2 v1 明确不做

- 不提供导游移动端、在线报名、候补、录取、导游确认或在线退出申请。
- 不读取或控制微信群，不保存群二维码，不开发微信机器人或自动发群。
- 不建立车辆库、司机库、酒店库、房间库或餐厅库。
- 不采集学生个人姓名、证件、联系方式或名单。
- 不接保险公司接口，不自动投保。
- 不提供工资、银行卡结算、支付、发票或财务功能；银行卡字段仅在未来实际结算需要且安全条件满足时启用。
- 不做多公司、多门店、多租户或互联网协同。
- 不做无人值守自动派团、路线优化或不可解释的导游自动排序。
- 不承诺任意格式 Word、Excel 和自由文本都能完全自动识别。
- 不承诺在未取得原始可编辑工作簿时像素级复刻现有 Excel 模板。

### 5.3 后续版本候选

- 稳定支持更多供应商非标准表格模板。
- 获得原始工作簿后进行模板级 Excel 样式、合并单元格和打印设置复刻。
- 多电脑协作、账号权限、服务端数据库和对象存储。
- 经合规和业务验证后的微信通知或保险适配接口。
- 更复杂的教师房、跨楼层偏好和餐厅特殊桌型规则。

这些候选不自动进入开发任务，必须单独立项和确认。

## 6. 核心对象与数据归属

### 6.1 逻辑数据模块

v1 使用一个 SQLite 数据库文件，通过数据访问层读写；“三个数据模块”是逻辑划分，不是三个数据库文件。

| 模块 | 核心对象 | 数据归属 |
| --- | --- | --- |
| 学校库 | 学校、学校联系人 | 跨项目复用 |
| 导游库 | 导游、证件、带团记录、退团记录 | 跨项目复用 |
| 项目业务数据 | 项目、班级、老师、项目导游、乘车行、住宿行、用餐行、导入、导出、异常和操作记录 | 归属于一个具体项目 |

### 6.2 数据关系原则

1. 项目是所有一次性业务资料和安排的归属单位。
2. 项目通过学校编号关联学校库，通过导游编号关联导游库。
3. 项目只保存导游关联和当次安排事实，不复制完整导游敏感档案。
4. 班级人数是车辆、住宿和用餐计算的共同来源。
5. 车辆和司机只存在于当前项目乘车安排中。
6. 酒店、房间、餐次和餐桌只存在于当前项目安排中。
7. 住宿和用餐只保存班级、性别、角色和人数，不保存具体学生或工作人员姓名。
8. 没有住宿或用餐时，不创建对应安排记录。
9. Excel 从项目当前业务数据生成，不作为与项目脱节的第二套数据源。
10. 已结束项目保留当时安排、导入导出和操作记录；档案后续修改不得抹去历史依据。
11. 学校或导游被历史项目引用后不得硬删除，只能按规则停用。

### 6.3 文件存储

- 结构化业务数据：一个 SQLite 文件。
- 导游证件图片：受限的本地证件目录，数据库只记录受控引用。
- Excel 导出：单独的业务导出目录。
- 备份：单独的受限备份目录，不与普通 Excel 导出混放。

## 7. 核心业务流程

```text
创建研学项目
  ↓
补充或导入学校、班级、老师和行程资料
  ↓
生成招募通知，负责人在微信群完成招募接龙
  ↓
粘贴最终接龙名单，匹配导游档案
  ↓
检查日期、证件和合作状态，分配班级
  ↓
导入或填写乘车安排，处理超载、拆班和遗漏
  ↓
按项目条件完成住宿和用餐安排
  ↓
处理阻断异常并重新校验
  ↓
预览并导出保险名单、乘车表及适用的住宿/用餐表
  ↓
出团、结束项目并保留历史记录
```

### 7.1 流程恢复原则

- 资料不完整时允许保存草稿，明确显示缺失项。
- 导入失败时不写正式数据，保留文件名、批次和错误原因。
- 自动建议无解时保留未安排项并说明缺口，不生成虚假完成结果。
- 人工修改后重新校验；旧预览立即标记过期。
- 导出失败不记录为成功，可在修正路径或保存位置后重试。
- 恢复失败不得把当前数据留在半覆盖状态；恢复前默认备份当前数据。

## 8. 功能需求

完整字段、按钮和逐页交互以 `页面字段与交互规则清单.md` 为准。本节定义模块结果和不能被 UI 改变的业务要求。

### 8.1 今日工作台与项目管理（P01、P02、P03、P04）

#### 项目初建

创建必填：

- 学校名称。
- 年级。
- 研学目的地。
- 出团日期。
- 结束日期。

行程类型由日期计算：同日为一日游，跨日为多日游，不再让用户重复选择互相矛盾的值。

项目允许后续补充集合地点、集合时间、行程内容、学校总负责人及电话、班级资料、住宿条件、用餐条件和特殊事项。

#### 项目列表与总览

- 列表至少显示学校、年级、目的地、出团/结束日期、状态、阻断异常和更新时间。
- 项目总览显示班级、学生、老师、所需/已安排导游、车辆及条件模块完成情况。
- 系统给出“继续下一步”，指向第一个未完成且适用的模块。
- 一日游或不住宿的项目将住宿标记为“不适用”；不含餐项目将用餐标记为“不适用”。
- 首页和项目总览只汇总和导航，不直接修改核心安排。

### 8.2 班级与老师（P05）

每个班级至少记录：

- 班级名称或编号。
- 学生总人数、男生人数、女生人数。
- 班主任姓名和电话。
- 配班老师姓名和电话，可重复多行。
- 随车老师人数。
- 需要导游人数。
- 特殊事项。

必须满足：

```text
班级学生总人数 = 男生人数 + 女生人数
项目学生总人数 = 所有班级学生总人数之和
```

班级名称在同一项目内唯一。班级人数改变后，车辆、住宿、用餐、异常、首页统计和表格预览全部重新计算。

### 8.3 学校库（P13）

- 学校库只保存学校编号、学校名称、常用联系人、联系电话和合作备注。
- 年级、班级、老师和学生人数属于具体项目，不写回学校库。
- 新建项目时可带入学校默认联系人，但项目联系人可以独立修改，不反向覆盖学校库。
- 已被历史项目引用的学校不能硬删除，只能停用；历史项目继续显示原关联。

### 8.4 招募通知与接龙导入（P06）

- 招募通知最少使用项目日期和所需导游人数。
- 学校名称和年级由负责人决定是否出现在通知中。
- 系统只生成和复制文本，不调用微信。
- 负责人粘贴最终接龙文本后，系统保存原始文本、批次和原始顺序。
- 导游自动匹配至少需要姓名和手机号同时一致。
- 同名、缺手机号、手机号冲突、档案不存在或资料不一致进入人工确认。
- 未建档人员必须建立导游档案并达到安排条件后，才能进入正式分配。

### 8.5 导游档案与项目安排（P07、P14、P15）

#### 导游档案

导游档案包括基础资料、紧急联系人、身份证资料、导游证资料、证件有效期、合作状态、带团和退团事实、备注，以及实际需要结算时才启用的银行卡信息。

普通项目安排页只显示姓名、工作电话、证件状态、合作状态和项目需要的信息，不直接展开身份证、证件图片或银行卡资料。

#### 安排规则

1. 项目人员名单保持接龙顺序，人数超出需求时由负责人决定保留或移出。
2. 导游在项目日期范围内已有其他安排时禁止重复分配，并显示冲突项目和重叠日期。
3. 证件过期、资料不完整到达阻断条件或合作停用的导游不可安排。
4. 一名导游默认负责一个班级。
5. 一名导游负责两个小班只能由负责人手工开启，并填写原因。
6. 更换导游后重新检查日期和班级覆盖。
7. 退团记录包含原因、记录时间、距出团天数和替换关系。
8. 临近出团退团次数从退团事实计算，不允许直接手改累计值。
9. 带团次数和好评次数应来自事实性记录，不用于不可解释的自动排序或派单。

### 8.6 统一资料导入（P11、P12）

支持的入口：

- Excel 文件。
- Word 文件。
- 复制粘贴文字。
- 手工录入。

导入流程必须是：

```text
选择项目和资料类型
  → 选择文件或粘贴文字
  → 解析到临时区
  → 字段映射和数据预览
  → 检查缺失、重复和格式
  → 负责人确认
  → 写入正式数据
```

要求：

- v1 优先稳定支持固定 Excel 模板和常见接龙文本。
- 非标准 Word、Excel 和自由文本必须允许人工修正映射。
- 不得跳过预览确认直接写入。
- 更新现有记录时显示匹配依据和覆盖字段，默认不以空值覆盖已有非空值。
- 每个批次保存原始文件名或“粘贴文字”、时间、项目、资料类型、成功、失败、跳过数量和错误原因。
- 批量结果区分成功、部分成功和失败。

### 8.7 乘车安排（P08）

车辆级字段至少包括车号、车队名称、车辆型号、可用座位数、车牌号、司机姓名、司机电话和备注。

每个乘员分配保留班级、本车学生人数、男女人数、班主任、配班老师、随车老师人数、导游及电话。一个车辆可以包含多个班级分配行，一个班级拆到多车时必须逐车记录实际人数。

计算规则：

```text
乘车人数 = 本车学生人数 + 随车老师人数 + 导游人数
空车位 = 可用座位数 - 乘车人数
```

自动建议顺序：

1. 一班一车优先。
2. 尽量不拆班。
3. 班级按既定顺序安排。
4. 在不超载的前提下减少座位浪费。
5. 同一学校尽量使用同一车队。

校验要求：

- 空车位可为 0，小于 0 为阻断异常。
- 拆班后各车学生人数合计必须等于班级总人数。
- 所有班级必须完成乘车安排。
- 导游只能计入其实际乘坐的一辆车。
- 缺少车牌、司机电话或其他导出必填字段时阻断正式导出。
- 自动建议覆盖人工结果前必须显示影响并由负责人确认。

### 8.8 住宿安排（P09）

启用条件：项目跨日且“需要住宿=是”。条件不满足时不显示入口、不创建数据、不导出空表。

房间资料至少包括酒店、楼栋、楼层、房间号、房型、可住人数和备注。分房记录包括班级、性别、人员角色、人数和跨班拼房信息。

住宿人数包括学生、导游、老师、司机、摄影师和其他需要住宿的随团人员，但不记录这些人员的具体姓名。

分房建议遵循：

1. 男生和女生分开。
2. 同班优先集中。
3. 班级按顺序、房号尽量连续。
4. 一个楼层无法容纳时再换楼层。
5. 低年级优先较低楼层。
6. 房间不得超容量。
7. 少量剩余学生可跨班拼房，但必须记录主班级、拼入班级、双方人数和原因。
8. 导游、老师、司机、摄影师和其他工作人员集中安排，并继续遵守性别和容量限制。

阻断异常包括总容量不足、房间超员、男女混住、班级人数不一致、人员遗漏和关键房间字段缺失。

### 8.9 用餐安排（P10）

启用条件：项目“是否含餐=是”。不含餐时不显示入口、不创建数据、不导出空表。

每一餐单独记录餐次、餐厅、用餐时间、每桌标准人数、实际预订桌数、桌号范围和备注。

基本规则：

- 学生和导游默认按每桌 10 人计算建议桌数。
- 老师和司机单独统计，并由负责人安排。
- 班级按顺序集中就座，同班桌号尽量连续。
- 一个班级最后不足一桌的人数可与后一班拼桌，必须分别记录各班人数。
- 同一餐次每个桌号唯一。
- 多家餐厅分别记录，不共用桌号和桌数统计。
- 餐次必须落在项目日期范围内。

学生与导游的基础建议：

```text
建议桌数 = 向上取整((学生人数 + 导游人数) / 每桌标准人数)
```

老师、司机与总预订桌数的最终口径在第 15 节确认前，不应把项目标记为正式完成。

### 8.10 表格中心与 Excel 输出（P16、P17）

固定输出：

- 保险名单：导游姓名、身份证号码、联系电话、导游证号码。
- 乘车安排表：车辆、班级、学生、老师、导游、司机、容量和备注字段及合计。

条件输出：

- 住宿项目生成住宿安排表。
- 含餐项目生成用餐安排表。

导出规则：

1. 导出前运行完整性和阻断异常检查。
2. 预览从项目当前数据生成，不能在预览中形成另一套独立业务数据。
3. 源数据变化后旧预览过期，必须重新生成。
4. 保险名单预览默认脱敏；导出完整号码前二次确认用途并记录日志。
5. 文件实际写入成功、存在、可由 Excel 打开且中文不乱码，才记录成功。
6. 没有业务数据时不导出空表。
7. 在未取得原始可编辑模板前，v1 验收字段、合计和业务结构，不承诺像素级复刻。

### 8.11 异常与记录（P18、P19）

异常等级：

| 等级 | 示例 | 处理规则 |
| --- | --- | --- |
| 阻断 | 日期冲突、超载、男女混住、人员未完全安排 | 必须修改源数据，不能忽略 |
| 警告 | 证件临期、车辆坐满、跨班拼房 | 可继续，但必须显著展示 |
| 提醒 | 选填资料缺失 | 不阻断，可导航补充 |

异常状态为待处理、已解决和已确认例外。只有警告和提醒可以由负责人填写原因后确认例外；阻断异常必须通过修改源数据并重新校验解决。

记录至少覆盖：

- 导入及批次结果。
- 查看、修改和导出敏感资料。
- 导游、车辆、住宿和用餐的人工调整。
- 表格导出及失败原因。
- 数据备份和恢复。

日志不得保存完整身份证号、银行卡号或证件图片地址。

### 8.12 本地备份与恢复（P20）

- 备份范围默认包括 SQLite 数据库和证件图片目录。
- 备份目录与普通 Excel 导出目录分开，并设置本地访问权限。
- 备份成功必须验证目标文件存在和清单完整。
- 恢复前显示备份时间、内容和覆盖影响，并默认先备份当前数据。
- 恢复操作必须记录；恢复失败不得留下半恢复状态。
- 自动备份频率、保留份数和默认目录由技术方案在产品默认值确认后确定。

## 9. 状态模型

### 9.1 项目状态

| 状态 | 含义 |
| --- | --- |
| 资料待补充 | 项目已建，但班级、老师或项目条件资料不完整 |
| 人员安排中 | 正在导入、匹配或分配导游 |
| 车辆安排中 | 正在填写或调整乘车安排 |
| 住宿安排中 | 适用项目正在安排住宿 |
| 用餐安排中 | 适用项目正在安排用餐 |
| 待检查 | 适用模块已初步完成，等待处理异常和最终检查 |
| 已完成安排 | 无阻断异常，负责人确认完成 |
| 已出团 | 项目已开始执行 |
| 已结束 | 项目结束，保留历史资料和记录 |

- 无住宿或用餐项目不出现对应状态。
- 候选默认采用“负责人推进状态＋系统门禁”：负责人决定推进，但系统阻止不满足条件的状态变化。
- “已完成安排”后修改班级人数、导游、车辆、住宿或用餐，项目自动退回“待检查”。

### 9.2 导游档案状态

- 合作中：可参与新项目安排。
- 暂停：暂不安排，但可恢复。
- 停用：不可参与新安排，历史记录保留。

证件状态独立为资料缺失、有效、临期、过期。合作状态正常不代表证件有效。

### 9.3 项目导游名单状态

- 待处理。
- 保留。
- 移出。
- 已退团。
- 已替换。

名单状态不等同于班级分配状态；保留但未分班仍属于人员安排未完成。

### 9.4 导入状态

- 待预览。
- 待确认。
- 成功。
- 部分成功。
- 失败。
- 已取消。

## 10. 跨模块联动

| 变化来源 | 必须更新的结果 |
| --- | --- |
| 项目日期 | 行程类型、住宿条件、导游日期冲突、项目列表和首页 |
| 班级人数 | 项目总人数、车辆核载、住宿、用餐建议、异常和表格合计 |
| 所需导游数 | 招募通知、人员缺口和首页指标 |
| 导游分配/退团/替换 | 班级覆盖、日期占用、车辆导游、保险名单和住宿人数 |
| 车辆或乘员 | 乘车人数、空车位、异常和乘车表 |
| 房间或住宿分配 | 容量、未分配人数、混住异常和住宿表 |
| 餐次或桌位 | 桌数差额、人员遗漏和用餐表 |
| 阻断异常解决 | 项目完成门禁、首页待办和表格可导出状态 |
| 任一源数据 | 对应旧预览过期，后续导出重新生成 |

所有联动必须以内部稳定编号建立关系，不能仅通过可能修改的班级名称、导游姓名或车号关联。

## 11. 数据导入与写入安全

1. 文件和粘贴文本先进入临时解析区，不直接写正式表。
2. 所有目标字段使用与页面清单一致的必填、格式和关联校验。
3. 重复记录需显示识别依据，负责人决定合并、跳过或返回修正。
4. 批量写入应使用事务，避免失败后留下无法解释的半批数据。
5. 若允许“只导入有效行”，必须记录部分成功、失败行和原因。
6. 同一导入批次重复提交时，应提示潜在重复，不静默新增相同业务记录。
7. 原始文件只按追查和合规所需保存；保存周期在数据保留策略确认后落地。

## 12. 隐私、安全与数据保护

### 12.1 数据最小化

- 学生只记录班级、性别和人数，不采集学生个人资料。
- 普通安排页只展示导游姓名和工作电话等必要信息。
- 身份证、导游证、银行卡和证件图片按用途最小化收集。

### 12.2 敏感信息

- 身份证号、导游证号和银行卡号默认脱敏。
- 证件图片默认不加载到普通列表或项目执行页。
- 查看完整值、修改和导出均记录时间、对象、结果和用途；日志本身不记录完整敏感值。
- 证件过期或合作停用时禁止新安排，但历史项目关联仍保留。

### 12.3 本地保护

- SQLite、证件目录和备份目录使用受限本地路径。
- 不允许通过页面直接编辑数据库文件。
- 不把数据库、证件目录或管理页面暴露到公网、局域网共享或临时隧道。
- 备份需要定期做恢复抽检；只生成备份文件不等于备份可用。

### 12.4 删除与保留

- 有历史项目引用的学校和导游不硬删除，只停用。
- 已结束项目保留当次安排、导入、导出和操作记录。
- 具体保留年限、证件图片删除条件和备份清理周期在第 15 节确认后落地。

## 13. 非功能要求

### 13.1 可用性

- 首版以桌面端为唯一验收端，不建设导游移动端。
- 1366×768 及以上桌面视口可完成核心操作，无页面级横向溢出；宽表可在自身容器内横向滚动。
- 状态和错误使用文字＋颜色，不只依赖颜色。
- 关键按钮说明操作结果；阻断时说明原因和修正入口。
- 未保存离开、删除引用数据、覆盖人工安排和恢复备份必须二次确认。

### 13.2 性能

- 在本地典型规模下，普通列表、筛选、保存和单模块校验目标为 1 秒内反馈。
- 大文件解析和 Excel 生成超过 1 秒时显示进行中状态，禁止重复提交。
- 典型规模需在技术方案中用真实业务样本补充定义，至少覆盖多班、多车、多房间和多餐次项目。

### 13.3 可靠性

- 批量导入、恢复和跨多表写入使用事务或等效原子性保护。
- 应用异常退出后，已确认保存的数据不丢失，临时未确认导入不得污染正式数据。
- 数据库结构变更前自动备份，并提供失败回滚路径。
- 导出成功必须以文件系统实际结果为证据。

### 13.4 可维护性

- 页面不直接读写 SQLite 文件，通过统一数据访问层处理。
- 校验规则集中定义，页面提示、异常中心和导出门禁使用同一规则结果。
- 学校、导游和项目关联使用稳定编号。
- 新增导入模板不能绕过预览、校验和操作记录。

## 14. v1 验收标准

本节列出产品级验收结果；实际验收必须按 `v1端到端验收标准.md`，使用同一个多日、住宿、含餐项目贯通 P01—P20。单个任务、页面或单元测试完成，不能替代全项目流程验收。

### A. 项目与人数

1. 只填写学校、年级、目的地和起止日期即可创建草稿项目。
2. 同日项目自动显示一日游，跨日自动显示多日游；结束日期早于出团日期时阻止保存。
3. 至少创建 3 个班级后，系统准确校验每班男女合计和项目总人数。
4. 修改任一班级人数后，车辆、住宿、用餐和表格预览均重新计算或标记过期。

### B. 导入与匹配

5. 标准 Excel 模板能够进入预览并在确认后写入；未确认前数据库不产生正式记录。
6. 非标准列名可以人工映射；缺失和格式错误精确定位到行列。
7. 同一批次包含有效行和错误行时，按已确认策略生成清晰的成功、部分成功或失败记录。
8. 接龙文本保持原始顺序；姓名和手机号同时一致自动匹配，同名、缺手机号和资料不一致进入人工确认。

### C. 导游安排

9. 日期冲突测试覆盖同日、交叉、包含和相邻但不重叠。
10. 证件过期或合作停用导游不能被分配。
11. 默认一名导游只能负责一个班级；双班例外强制填写原因。
12. 退团后原班级显示缺口，替换关系和距出团天数可追查。

### D. 车辆

13. 乘车人数准确包含本车学生、随车老师和导游，空车位可以为 0 但不能为负。
14. 自动建议优先不拆班；座位不足时保留未安排班级并显示缺少座位数。
15. 一个班级拆到多车时，各车学生合计必须等于班级总人数。
16. 人工调整车辆后，容量和异常立即重新校验。

### E. 住宿与用餐

17. 一日游、不住宿项目不创建住宿数据，也不出现住宿表导出。
18. 住宿建议不超容量、不混住；跨班拼房显示双方班级、人数和原因。
19. 含餐项目按餐次、餐厅和唯一桌号记录，学生＋导游桌数建议计算正确。
20. 不含餐项目不创建用餐数据，也不出现用餐表导出。

### F. 异常、导出和安全

21. 阻断异常只能通过修改源数据解决；警告例外必须填写原因。
22. 保险、乘车及适用的住宿/用餐预览都来自项目当前数据，旧预览在源数据变化后不可继续导出。
23. 导出的 Excel 文件实际存在、可打开、中文不乱码，字段和合计符合本 PRD 与页面规则清单。
24. 查看或导出完整证件信息产生不含完整敏感值的操作记录。
25. 已被历史项目引用的学校和导游不能硬删除。
26. 完整备份包含 SQLite 和证件目录；恢复测试成功，恢复失败可回到恢复前数据。

### G. 页面与范围

27. `页面字段与交互规则清单.md` 中 P01—P20 均有对应入口或条件入口。
28. 1366×768 桌面视口可完成项目创建、接龙导入、导游安排、乘车、条件住宿/用餐和导出关键路径。
29. 产品中不存在导游移动端、在线报名、独立车辆/酒店资料库或学生个人名单入口。
30. 现有静态 Demo 只有在逐项通过本节验收后，才能标记为符合新版 PRD；过去测试结果不能自动沿用。

## 15. 待确认产品决策

以下推荐值用于形成完整候选方案。用户确认后将写入决策记录并把 PRD 状态改为 `APPROVED`。

| 编号 | 决策项 | 推荐默认值 | 不确认的影响 |
| --- | --- | --- | --- |
| D01 | 临近出团退团阈值 | 距出团 3 天及以内 | 只能记录原始天数，不能计算临近退团次数 |
| D02 | 工作人员住宿人数入口 | 在 P09 设置项目级角色汇总区 | 住宿总人数来源不完整 |
| D03 | 老师、司机用餐桌数口径 | 学生＋导游计算基础建议；老师、司机单列人数和人工追加桌数，最终总容量覆盖全部就餐人员 | 无法准确判断最终预订桌数是否足够 |
| D04 | 项目状态推进方式 | 负责人手动推进＋系统阻断门禁 | 状态变化和按钮条件不确定 |
| D05 | 错误行导入策略 | 允许仅导入有效行，记录为部分成功并保留失败原因 | 批量导入遇一行错误时的结果不确定 |
| D06 | 停用、删除和保留 | 有历史引用不硬删除；无引用误建数据可删除；不凭空设定保留年限，真实使用前由公司依据业务和适用要求确认业务记录、证件图片及备份的保留周期 | 数据清理和历史追查规则不完整，真实敏感数据不能进入系统 |
| D07 | 证件临期提醒 | 到期前 30 天提醒 | 首页提醒时间不确定 |
| D08 | 备份策略 | 每日首次退出前提示备份；保留最近 30 个日备份和 12 个按月备份；具体目录技术设计时确定 | 备份页面与存储容量无法验收 |
| D09 | 好评事实口径 | 仅允许负责人关联已结束项目并依据可核实反馈记录，可纠错，不生成信用分或自动排序 | 好评次数缺少可靠数据来源 |

### 15.1 发布前安全决策

以下不是可后置的产品偏好，而是真实敏感数据进入系统前的发布门禁：

- 明确采用专用 Windows 账户还是应用解锁机制。
- 确认证件和备份目录访问权限。
- 完成一次备份与恢复演练。
- 明确证件图片保留、删除和事故处理责任人。

## 16. 风险与依赖

| 风险/依赖 | 影响 | 应对 |
| --- | --- | --- |
| 来源表格格式不统一 | 自动识别准确率不可控 | 固定模板优先，字段映射、预览和人工确认兜底 |
| 缺少原始可编辑 Excel 模板 | 无法验证公式、合并单元格和打印格式 | v1 验收字段、合计和结构；模板级复刻后置 |
| 班级人数频繁变化 | 下游安排和已导出表失效 | 单一人数来源、联动重算、预览过期标记 |
| 自动安排存在业务例外 | 无法承诺无人值守完成 | 建议＋人工调整＋持续校验 |
| 本地保存真实证件 | 存在泄露和误删风险 | 最小化、脱敏、访问控制、日志、备份和恢复门禁 |
| 单机故障 | 可能导致全部业务数据丢失 | 独立备份目录、定期恢复抽检 |
| 旧 Demo 与新范围不一致 | 容易把旧测试误认为新 PRD 验收 | 按第 14 节重新审查，旧通过项只作参考 |

## 17. 上线准备与验收证据

### 17.1 文档门禁

- 用户确认第 15 节决策。
- PRD 状态改为 `APPROVED`。
- `页面字段与交互规则清单.md` 同步更新已确认结果。
- 再依据两份已确认文档重建正式 `tasks.md`。

### 17.2 开发验收证据

- 按 `v1端到端验收标准.md` 完成同一项目端到端主流程及必要补充分支。
- 数据结构和迁移检查。
- 日期、人数、容量、条件模块和状态门禁单元测试。
- 导入成功、部分成功、失败和重复提交测试。
- Excel 文件存在、可打开、字段合计和中文编码检查。
- 关键桌面流程浏览器测试和 1366×768 人工检查。
- 敏感信息脱敏与日志检查。
- 备份文件完整性及恢复演练记录。

### 17.3 当前就绪判断

- PRD 文档：已形成可审查候选版。
- 产品规则：`Ready with assumptions`，第 15 节推荐默认值待用户确认。
- 正式开发任务：尚未生成；应在 PRD `APPROVED` 后重建。
- 现有 Demo：只能作为历史交互样例，尚未证明满足本 PRD。
